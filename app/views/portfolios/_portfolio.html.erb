<p><%= portfolio.title %></p>
<%= link_to 'Show', portfolio %>
<%= link_to 'Edit', edit_portfolio_path(portfolio) %>
<td><%= link_to 'Destroy', portfolio, method: :delete, data: { confirm: 'Are you sure?' } %></td>
<p><%= portfolio.body %></p>
<p><%= portfolio.subtitle %></p>
<img src="<%= "#{portfolio.thumbnail_image}"%>">